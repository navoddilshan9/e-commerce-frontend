class Admin {
  constructor(adminId, adminName, password, email, userName) {
    this.adminId = adminId;
    this.adminName = adminName;
    this.password = password;
    this.email = email;
    this.userName = userName;
  }
}

export default Admin;
